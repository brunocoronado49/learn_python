# Debe contener mas de un elemento para ser nua tupla
tup = (1, 2, 3, 4, 5)
print(tup)

# Constructor de tupla
con = tuple(("enero", "febrero", "marzo", "abril", "mayo", "junio"))
print(con)

# Muestra los metodos y funciones de las tuplas
print(dir(tup))